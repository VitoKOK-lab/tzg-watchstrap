ADMIN_EMAILS=luxkey.tw@gmail.com
# Server-only. Add to Vercel environment variables; never use NEXT_PUBLIC_.
# Current sender: luxkey.tw@gmail.com. Google app password, NOT account password.
# Gmail takes priority when configured. Sender and both recipients are fixed.
GMAIL_APP_PASSWORD=
# Optional alternative, only for a verified Resend sending domain:
RESEND_API_KEY=
# A sending address on a verified Resend domain, without a display name.
INQUIRY_FROM_EMAIL=
